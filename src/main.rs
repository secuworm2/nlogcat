#![warn(clippy::all)]
#![warn(clippy::pedantic)]
#![allow(clippy::module_name_repetitions)]

mod app;
mod adb;
mod engine;
mod model;
mod ui;
mod config;
mod theme;

fn main() {
    // eframe 진입점 (TASK-09에서 구현)
}
